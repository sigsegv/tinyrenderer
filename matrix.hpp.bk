#pragma once

template<typename T, unsigned kRows, unsigned kCols>
class matrix
{
public:
    /**
     * Create empty matrix
     */
    matrix() : m{{0}}
    {
    }
private:
    T m[kRows][kCols];
};

using matrix4f = matrix<float, 4, 4>;

void matrix_test()
{
    matrix4f m0;
}
